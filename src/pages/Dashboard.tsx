// src/pages/Dashboard.tsx
// This is the landing page of the app.
// For now it's just a shell — we'll populate it once routing is confirmed working.
// Keeping page components thin (they orchestrate, not implement) is good practice.

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Top 10 cryptocurrencies will appear here.</p>
    </div>
  );
};

export default Dashboard;